name: CD

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Trigger Deploy
        run: |
          curl -X GET https://api.render.com/deploy/srv-d4g5e3a4d50c73f76td0?key=CPVYR5e7eRM
